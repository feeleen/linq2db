
jobs:
- job: RunInContainer
  pool:
    vmImage: 'vs2017-win2016'

  steps:
    - checkout: none
    - powershell: |
        $dbName = "TestDatanet45"
        $cmd = '"C:\Program Files\MySql\MySQL Server 5.7\bin\mysql" -e "create database $dbName;" --user=root --p=Password12!'
        iex "& $cmd"

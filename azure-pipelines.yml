
jobs:
- job: RunInContainer
  pool:
    vmImage: 'vs2017-win2016'

  steps:
    - checkout: none

    - powershell: |
        $env:MYSQL_PWD="Password12!"
        $dbName = "TestDatanet45"

        choco install mysql --params "/port:3307 /installLocation:'C:'"
        $cmd = '"C:\mysql\current\bin\mysql" -e "create database $dbName;" --user=root --port=3307 --password=Password12!'
        iex "& $cmd"
      displayName: Install mysql

    - script: printenv
